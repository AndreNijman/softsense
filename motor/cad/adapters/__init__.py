"""Mounting adapters — printed PA12-GF parts that mate the gripper's bottom
M4 flange to a specific arm/ROV/chassis interface family.

One module per adapter family; all share the gripper-side mating geometry
from `_base.py`. Run any module as a script to emit its STEP into
`motor/cad/output/`.
"""
