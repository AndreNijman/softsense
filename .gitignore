__pycache__/
*.glb
node_modules/

# FEA/render transfer scratch (MSI remote workflow)
fea_out.tgz
fea_pull/

# FEA iteration scratch — large/regenerable (keep eval.json/metrics.json + renders)
fea/iterations/**/*.npz
fea/iterations/**/section.step
fea/iterations/_*.log

# Render-frame scratch produced by scripts/render_full_system.sh
motor/cad/output/frame_*.step

# scaled-variant build logs (regenerable)
variants/**/_*.log

# Claude Code scratch worktrees (the workflow scripts under .claude/ are kept)
.claude/worktrees/

# Assembly-animation scratch: ~2 GB of per-frame STEP/GLB/PNG + logs.
# The finished assembly mp4/gif are checked in under renders/.
anim/

# Intermediate render scratch (regenerable; the composed gifs/mp4s and
# summary stills are kept).
renders/**/_frames_*/
renders/_*/
grip/iterations/_hqcache/

# Firmware build log (PlatformIO)
firmware/build.log

# Dated .docx backups (the live document is tracked).
*.backup-*.docx
